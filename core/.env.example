# Core Service Settings
SECRET_KEY=django-insecure-development-key-replace-me
DEBUG=True

# Database Connection (Docker default)
DB_HOST=db
DB_PORT=5432
DB_NAME=code_of_clan_db
DB_USER=postgres
DB_PASSWORD=postgres

# Redis & Cache
REDIS_URL=redis://redis:6379/0

# Allowed Hosts & URLs
ALLOWED_HOSTS=localhost,127.0.0.1,core
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost

# Email (SMTP for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=no-reply@localhost

# OAuth (Placeholders)
GITHUB_CLIENT_ID=your_id
GITHUB_CLIENT_SECRET=your_secret
GOOGLE_CLIENT_ID=your_id
GOOGLE_CLIENT_SECRET=your_secret

# JWT (Base64 keys for easy dev setup)
# Replace these with real keys for signing in local production testing
JWT_PRIVATE_KEY=""
JWT_PUBLIC_KEY=""
